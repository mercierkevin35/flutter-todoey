import 'package:flutter/material.dart';

const TextStyle kListTextStyle = TextStyle(
  fontSize: 20.0,
);

const double kBorderRadiusValue = 20.0;

const BorderRadius kBorderRadius = BorderRadius.only(
    topLeft: Radius.circular(kBorderRadiusValue),
    topRight: Radius.circular(kBorderRadiusValue));
